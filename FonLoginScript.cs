using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FonLoginScript : MonoBehaviour
{
   
    void Start()
    {
        //AppManager.Instance.userInfo.user.phone = PlayerPrefs.GetString("phoneNumber", AppManager.Instance.userInfo.user.phone);
    }

   
}
